<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Users</title>
  </head>
  <body>
    <h1>Users</h1>
    <a href="/add">Add a User</a>
    {{#users}}
    <p> Name: <a href="/update/{{id}}">{{name}}</a></p>
    <p> Email: {{dataValues.email}} </p>
    <p> Bio: {{dataValues.bio}} </p>
    <p> Id: {{dataValues.id}} </p>
    <form class="delete" action="/delete/{{id}}" method="post">
      <button type="submit" name="button">Delete</button>
    </form>
    {{/users}}
    {{^users}}
    No Users To Show
    {{/users}}
  </body>
</html>
